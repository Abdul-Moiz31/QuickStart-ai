# Web reads env from the monorepo root — copy ../../.env.example to ../../.env
# Do not duplicate secrets here. For Vercel, set NEXT_PUBLIC_* in the project dashboard.

NEXT_PUBLIC_API_URL="http://localhost:3100"
NEXT_PUBLIC_APP_NAME="QuickStart AI"
NEXT_PUBLIC_DEMO_CLIENT_ID=""
